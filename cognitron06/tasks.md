# Code Quality Improvement Checklist

> Use this checkable list to incrementally improve the codebase. Each item includes clear actions and a concrete validation step (tests/commands). Check off once validated.

- [x] Fix SSE parameter bug and headers
  - Change `_generate_streaming_response` call in `server/src/api/chat.py` to pass `api_params` (not `api_options`).
  - Set `StreamingResponse` media type to `text/event-stream`; include headers: `Cache-Control: no-cache`, `Connection: keep-alive`, `X-Accel-Buffering: no`.
  - Validation:
    - Run: `cd server && pytest -q -k test_api_endpoints` (should pass existing API tests).
    - Manual: `curl -N -H "Authorization: Bearer <TOKEN>" -H "Content-Type: application/json" -d '{"message":"hello","stream":true}' http://localhost:8000/api/v1/chat/stream | head` (see incremental `data:` lines).

- [x] Add public memory accessors and refactor API usage
  - In `MemGPTMemorySystem`, add `get_current_session_id(user_id)` and `get_user_memory_snapshot(user_id)`.
  - Refactor `server/src/api/chat.py` and `server/src/api/websocket.py` to use these instead of `._get_user_memory(...)`.
  - Validation:
    - Add unit test `server/tests/test_memory_accessors.py` covering accessor behavior.
    - Run: `cd server && pytest -q -k memory_accessors`.

- [x] Harden error responses (avoid leaking internals)
  - In `server/src/main.py` global exception handler, return a generic message when `settings.debug` is False; log details internally.
  - Validation:
    - Add test that triggers an exception (e.g., temporary route in a test app) and asserts response hides `detail` when `debug=False`.
    - Run: `cd server && pytest -q -k error_handler`.

- [x] Make CORS configurable and safe for prod
  - Read allowed origins from env (e.g., `COGNITRON_CORS_ORIGINS` as CSV) and default to `*` only in debug.
  - Validation:
    - Unit test parses env into `settings.cors_origins` list.
    - Optional: Start server with `COGNITRON_CORS_ORIGINS=http://localhost:3000` and verify preflight.

- [x] Add integration test for SSE streaming
  - Create `server/tests/test_streaming_sse.py` using `httpx.AsyncClient` against `/api/v1/chat/stream` to assert:
    - `content-type` is `text/event-stream`.
    - At least one `data:` chunk arrives, then a completion marker.
  - Validation: `cd server && pytest -q -k streaming_sse`.

## Docker Rebuild Note
- Always rebuild the server container with no cache to validate changes:
  - `docker compose down && docker compose build --no-cache cognitron06-server && docker compose up -d --force-recreate cognitron06-server`

- [x] Unify context building across streaming and non-streaming
  - Extract a helper in `ChatAgent` to build messages (`build_messages(user_id, user_text=None, include_tools=<bool>)`). Use in both `generate_response` and `_generate_streaming_response` call sites (SSE and WS handlers).
  - Validation:
    - Run existing tests: `cd server && pytest -q`.
    - Spot-check manual chat and stream produce consistent content prefixes.

- [x] Improve client error propagation
  - In client modules, preserve original error using `cause` (Node 16+) and attach `response` body where available.
  - Example: `throw new Error('Chat request failed', { cause: error });` and keep Axios `error.response?.data` in a `.data` field.
  - Validation:
    - Add unit tests in `client/tests/error.test.js` to verify error `cause` support; extend later to Axios mocks.
    - Run: `cd client && npm test -s -- tests/error.test.js`.

- [x] Fix broken client bin entry
  - Either add `client/test-arrow-keys-direct.js` or remove the `"cognitron-test"` bin from `client/package.json`.
  - Validation:
    - Run: `cd client && node -p "Object.keys(require('./package.json').bin)"` (should not include missing files).

- [x] Add ESLint configuration for client
  - Add `.eslintrc.json` (env: node, es2022; extends: recommended) and align `npm run lint` to fail on errors.
  - Validation:
    - Run: `cd client && npm run lint` (expect zero errors or actionable list).

- [x] Add Ruff configuration for server
  - Add `server/ruff.toml` with sensible rules (E,F,I). Integrate `ruff check src` into dev docs.
  - Validation:
    - Run: `cd server && ruff check src` (expect no violations or fix them).

- [ ] Strengthen session management
  - Add simple rate-limiting or failed login backoff in `SessionManager` to mitigate brute force.
  - Validation:
    - Add test `server/tests/test_auth_rate_limit.py` simulating rapid failed logins and asserting block/backoff.
    - Run: `cd server && pytest -q -k auth_rate_limit`.
  - Status: Implemented in-memory backoff (5 fails in 60s → 60s block) with tests.

- [x] Document security/config guidance
  - Update `README.md` and `docs/quick-start.md` with: env vars (`COGNITRON_SECRET_KEY`, CORS), production flags, and warning about autogenerated secrets.
  - Validation:
    - Peer-review PR checklist includes “Docs updated” and a quick local doc build visual check.

- [x] WebSocket parity test
  - Add `server/tests/test_websocket_streaming.py` using `websockets` to assert basic stream lifecycle: `stream_start` → `stream_chunk` → `stream_end` and error path with tools.
  - Validation: `cd server && pytest -q -k websocket_streaming`.

- [x] Optional: Persist users/sessions
  - Abstract `users_db` to a storage interface; provide a file- or SQLite-backed implementation for non-demo use.
  - Validation:
    - Add tests asserting user creation, password hashing/verification, and session validation across process restarts.
    - Run: `cd server && pytest -q -k session_persistence`.

- [x] Optional: CI sanity pipeline
  - Add GitHub Actions to run `ruff`, `pytest`, and `npm test` on PRs.
  - Validation:
    - Open a PR and confirm green checks for Python and Node workflows.

---

Notes
- Prefer small, reviewable PRs: one checklist item per PR when feasible.
- Keep test names aligned with `pytest.ini` markers (`unit`, `integration`, `websocket`, `api`, `memory`).
- For manual streaming checks, ensure the server is running and you have a valid Bearer token from `/api/v1/auth/login` (demo creds: `demo/demo123`).
